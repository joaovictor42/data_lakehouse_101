# Catálogo "postgres": o sistema transacional "fonte", consultável
# ao vivo pelo Trino — permite até fazer JOIN entre a fonte e o
# data lake numa única query.
#
# IMPORTANTE: mantenha estes valores iguais aos do .env
# (POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB).
connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/database
connection-user=trilha
connection-password=trilha123
